    <nav class="navbar navbar-default navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="{{ path_for('home') }}">Kartflohmarkt 2.0</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
            <li class="{{ routeName == 'home' ? 'active' : '' }}"><a href="{{ path_for('home') }}">Home</a></li>
            <li class="{{ routeName == 'about' ? 'active' : '' }}"><a href="{{ path_for('about') }}">About</a></li>
            <li class="{{ routeName == 'impressum' ? 'active' : '' }}"><a href="{{ path_for('impressum') }}">Impressum</a></li>
            {% if auth.check %}
              <li><a href="{{ path_for('auth.logout') }}">Logout</a></li>
              <li class="{{ routeName == 'auth.change' ? 'active' : '' }}"><a href="{{ path_for('auth.change') }}">Passwort Ändern</a></li>
            {% else %}
              <li class="{{ routeName == 'auth.signup' ? 'active' : '' }}"><a href="{{ path_for('auth.signup') }}">Registrieren</a></li>
              <li class="{{ routeName == 'auth.signin' ? 'active' : '' }}"><a href="{{ path_for('auth.signin') }}">Anmelden</a></li>
            {% endif %}
          </ul>
        </div>
      </div>
    </nav>